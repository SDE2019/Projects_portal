@model ProjectProgressMonitoringSystem.Models.CreateGroup1

@{
    ViewBag.Title = "Index";
}
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <h2>Create Group</h2>
    <div class="row">
        <div class="col-lg-4 mx-auto text-center">
            <fieldset>

                <div class="form-group">
                    @Html.DropDownList("StudentUSN", null, "Select Student", htmlAttributes: new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.StudentUSN, "", new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    @Html.DropDownList("StudentUSN", null, "Select Student", htmlAttributes: new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.StudentUSN, "", new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    @Html.DropDownList("StudentUSN", null, "Select Student", htmlAttributes: new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.StudentUSN, "", new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    @Html.DropDownList("StudentUSN", null, "Select Student", htmlAttributes: new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.StudentUSN, "", new { @class = "text-danger" })
                </div>
             
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Create" class="btn btn-default" />
                    </div>
                </div>
            </fieldset>
        </div>


    </div>
}
